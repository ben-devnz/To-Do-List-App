<Window
    x:Class="To_Do_List_App.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:To_Do_List_App"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="clr-namespace:To_Do_List_App.ViewModel"
    Title="MainWindow"
    Width="800"
    Height="450"
    mc:Ignorable="d">
    <Window.DataContext>
        <vm:MainViewModel />
    </Window.DataContext>
    <Grid Margin="20" Background="#E0E0E0">

        <Label
            Content="To Do List"
            FontSize="20"
            FontWeight="Bold" />
        <StackPanel HorizontalAlignment="Right" Orientation="Horizontal">
            <Button
                Padding="10"
                HorizontalAlignment="Right"
                VerticalAlignment="Top"
                Background="#E0E0E0"
                BorderBrush="#CFCFCF"
                BorderThickness="1"
                Command="{Binding AddTaskCommand}"
                Content="Add Task" />
            <Button
                Padding="10"
                HorizontalAlignment="Right"
                VerticalAlignment="Top"
                Background="#E0E0E0"
                BorderBrush="#CFCFCF"
                BorderThickness="1"
                Command="{Binding DeleteTaskCommand}"
                Content="Delete Task" />

            <Button
                Padding="10"
                HorizontalAlignment="Right"
                VerticalAlignment="Top"
                Background="#E0E0E0"
                BorderBrush="#CFCFCF"
                BorderThickness="1"
                Command="{Binding ToggleCompleteCommand}"
                Content="Mark As Complete" />
        </StackPanel>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="40" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <ListView
                Name="UserList"
                Grid.Row="1"
                Margin="20"
                Background="#E0E0E0"
                BorderBrush="#CDCDCD"
                BorderThickness="1"
                ItemsSource="{Binding Tasks}"
                SelectedItem="{Binding SelectedTask}">
                <ListView.View>
                    <GridView>
                        <GridViewColumn DisplayMemberBinding="{Binding Name}" Header="Task" />
                        <GridViewColumn DisplayMemberBinding="{Binding Details}" Header="Details" />
                        <GridViewColumn DisplayMemberBinding="{Binding IsCompleted}" Header="Completed?" />
                        <GridViewColumn DisplayMemberBinding="{Binding CreatedDate}" Header="Created" />
                    </GridView>
                </ListView.View>
            </ListView>
        </Grid>
    </Grid>
</Window>
